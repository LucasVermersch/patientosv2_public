{% extends 'base.html.twig' %}

{% block title %}Hello DashboardController!{% endblock %}

{% block body %}

    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
        google.charts.load('current', {'packages': ['corechart']});
        google.charts.setOnLoadCallback(drawChart);

        function drawChart() {
            var data = google.visualization.arrayToDataTable([
                ['Date', 'Nombres de lits'],
                ['J-7', {{ tabJour[6] }}],
                ['J-6', {{ tabJour[5] }}],
                ['J-5', {{ tabJour[4] }}],
                ['J-4', {{ tabJour[3] }}],
                ['J-3', {{ tabJour[2] }}],
                ['J-2', {{ tabJour[1] }}],
                ['J-1', {{ tabJour[0] }}],
                ['J-J', {{ nbOccupee }}],
                ['J+1', {{ tabJours[0] }}],
                ['J+2', {{ tabJours[1] }}],
                ['J+3', {{ tabJours[2] }}],
                ['J+4', {{ tabJours[3] }}],
                ['J+5', {{ tabJours[4] }}],
                ['J+6', {{ tabJours[5] }}],
                ['J+7', {{ tabJours[6] }}],

            ]);

            var options = {
                title: 'Nombre de lits occupés',
                hAxis: {title: 'jours', titleTextStyle: {color: '#333'}},
                vAxis: {minValue: 0}
            };

            var chart = new google.visualization.AreaChart(document.getElementById('chart_div'));
            chart.draw(data, options);
        }
    </script>
    <script type="text/javascript">
        google.charts.load('current', {'packages': ['corechart']});
        google.charts.setOnLoadCallback(drawChart);

        function drawChart() {
            var data = google.visualization.arrayToDataTable([
                ['Date', 'Nombres de lit'],


            ]);

            var options = {
                title: 'Company Performance',
                hAxis: {title: 'Year', titleTextStyle: {color: '#333'}},
                vAxis: {minValue: 0}
            };

            var chart = new google.visualization.AreaChart(document.getElementById('chart_divv'));
            chart.draw(data, options);
        }
    </script>

    <div align="center">
        <div class="form-group">
            <div id="modifier_class" class="col-lg-9">
                <div class="col-md-12 mb-3">
                    <h1 class="titre">HOPITAL LILLE</h1>
                    <hr class="my-4">
                    <p>
                        {% for message in app.flashes('success') %}
                    <div class="alert alert-success">
                        {{ message }}
                    </div>
                    {% endfor %}

                    </p>

                        <div class="container" align="center">
                            <div class="row">
                                <div class="col-sm-12 ">
                                            <div class="card border-dark mb-3 ml" >
                                                <p>Lit disponible demain</p>
                                                <h1 class="display-5">{{ nbDispoDemain }}</h1>
                                            </div></div>

                                <div class="col-sm-12">
                                    <div class="card border-dark mb-3 ml" >
                                        <p>Taux de remplissage lit </p>
                                        <h1 class="display-5">{{ taux |round(1)}}%</h1>
                                  <!--      <div class="progress">
                                            <div class="progress-bar bg-danger" role="progressbar" style="width: 100%"
                                            aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>
                                            </div> -->
                                        </div>
                            </div>
                                    <div class="col-sm-12">
                                        <div class="card border-dark mb-3 ml">
                                            <div class="my-auto" id="chart_div"></div>
                                            </div>
                                        </div>
                                    </div>
                        </div>
                </div>
            </div>
        </div>
    </div>












{% endblock %}
